================================================================
EXPLICATION INTENSIVE DES TECHNOLOGIES DU PROJET MLOPS
================================================================

1. DOCKER & DOCKER COMPOSE
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
Docker est une plateforme de "conteneurisation". Imaginez des "boîtes" virtuelles légères qui contiennent tout ce dont votre application a besoin pour fonctionner (code, librairies, version de Python, système d'exploitation minimal).

> POURQUOI L'UTILISER ?
Le problème classique en informatique est : "Ça marche sur ma machine, mais pas sur la tienne". Docker résout ça. Si ça marche dans le conteneur Docker, ça marchera partout (Windows, Linux, Mac, Cloud).

> DANS CE PROJET :
Toute notre infrastructure tourne dans des conteneurs isolés définis dans le fichier `docker-compose.yaml`.
- Nous avons 4 conteneurs qui tournent en même temps :
  1. Postgres (Base de données)
  2. MLflow (Serveur de tracking)
  3. Airflow Webserver (Interface)
  4. Airflow Scheduler (Moteur)
Docker Compose permet de lancer ces 4 services avec une seule commande : `docker-compose up`.

2. APACHE AIRFLOW
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
C'est un outil d'orchestration de workflow. Il permet de programmer, planifier et surveiller des séquences de tâches complexes (appelées DAGs - Directed Acyclic Graphs).

> POURQUOI L'UTILISER ?
En Data Science, on a souvent des pipelines : "Télécharger les données" -> "Nettoyer" -> "Entraîner" -> "Evaluer". Faire ça à la main est fastidieux et source d'erreurs. Airflow automatise tout ça.

> DANS CE PROJET :
Airflow est le "Cerveau" du système.
- Il exécute le script `continuous_retraining_dag.py` tous les jours.
- Il gère la logique : "Si nouvelles données -> Alors entraîner -> Si meilleur -> Alors déployer".
- Il gère les erreurs : si une étape plante, il peut réessayer ou vous envoyer une alerte.

3. MLFLOW
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
C'est une plateforme open-source pour gérer le cycle de vie du Machine Learning. Elle permet de "tracker" (suivre) chaque expérience.

> POURQUOI L'UTILISER ?
Quand on entraîne un modèle, on change souvent des paramètres (learning rate, epochs...). On finit par avoir 50 versions et on ne sait plus laquelle est la meilleure. MLflow note tout pour vous.

> DANS CE PROJET :
MLflow agit comme le "Carnet de notes" du laboratoire.
- À chaque entraînement lancé par Airflow, MLflow enregistre :
  - Les hyperparamètres utilisés.
  - La précision (Accuracy) obtenue.
  - Le fichier du modèle (.pth).
- C'est grâce à lui qu'on peut comparer l'ancien et le nouveau modèle pour décider si on déploie.

4. DVC (DATA VERSION CONTROL)
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
C'est "Git pour les données". Git est génial pour le code (texte léger), mais nul pour les gros fichiers (images, vidéos). DVC permet de versionner des gigaoctets de données tout en utilisant Git.

> POURQUOI L'UTILISER ?
Pour la reproductibilité. Si vous ré-entraînez un modèle dans 6 mois, vous devez être sûr d'utiliser EXACTEMENT les mêmes données qu'aujourd'hui.

> DANS CE PROJET :
DVC suit notre dossier d'images `data/chest_xray`.
- Le fichier `chest_xray.dvc` est un petit fichier texte qui contient le "hash" (l'empreinte digitale) des données.
- Ce petit fichier est partagé sur GitHub.
- Les vraies images sont stockées à part (cache local ou cloud), et DVC les "télécharge" quand on en a besoin.

5. PYTORCH (TORCH & TORCHVISION)
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
C'est un framework de Deep Learning développé par Facebook (Meta). Il permet de créer et d'entraîner des réseaux de neurones.

> POURQUOI L'UTILISER ?
Il est très flexible, facile à débugger, et très populaire dans la recherche.

> DANS CE PROJET :
- Nous utilisons le modèle **ResNet18** (Residual Network à 18 couches).
- Nous utilisons le **Transfer Learning** : on prend un ResNet18 déjà intelligent (entraîné sur ImageNet) et on lui apprend juste à reconnaître la pneumonie. Cela permet d'avoir d'excellents résultats avec peu de données et un entraînement rapide.

6. DJANGO
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
C'est un framework web complet en Python ("Batteries included"). Il permet de créer des sites web robustes et sécurisés rapidement.

> POURQUOI L'UTILISER ?
Pour transformer notre modèle mathématique en une vraie application utilisable par des humains. Un médecin ne veut pas lancer un script Python, il veut un site web.

> DANS CE PROJET :
- Django sert l'interface graphique (HTML/CSS).
- Il gère l'upload sécurisé des images radios.
- Il charge le modèle PyTorch en arrière-plan et fait la prédiction quand on clique sur "Analyser".

7. HEROKU
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
C'est une plateforme Cloud (PaaS - Platform as a Service). Elle permet de mettre un site web en ligne sans gérer de serveurs complexes.

> POURQUOI L'UTILISER ?
Pour rendre le projet accessible au monde entier. C'est l'étape finale du MLOps : le déploiement en production.

> DANS CE PROJET :
- Heroku héberge notre code Django.
- Il redémarre l'application automatiquement quand Airflow lui envoie une nouvelle version du modèle via Git (`git push heroku master`).

8. POSTGRESQL
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
C'est un système de gestion de base de données relationnelle très puissant et fiable.

> DANS CE PROJET :
C'est la base de données technique utilisée par Airflow et MLflow pour stocker leurs informations internes (historique des jobs, utilisateurs, configurations, métadonnées des runs).

================================================================
RÉSUMÉ DE L'INTERACTION
================================================================
1. Le Code est sur **GitHub**.
2. Les Données sont gérées par **DVC**.
3. **Airflow** (dans **Docker**) détecte les changements.
4. Il lance **PyTorch** pour entraîner.
5. Il loggue les résultats dans **MLflow**.
6. Si c'est bon, il déploie le tout sur **Heroku** via **Git**.
7. L'utilisateur final utilise **Django**.
================================================================

9. CI/CD (CONTINUOUS INTEGRATION / CONTINUOUS DELIVERY)
----------------------------------------------------------------
> QU'EST-CE QUE C'EST ?
Ce sont deux pratiques devops fondamentales pour automatiser la mise en production.

> CI (CONTINUOUS INTEGRATION) :
C'est le fait de fusionner r�guli�rement le code de tous les d�veloppeurs dans une branche commune.
- But : D�tecter les bugs tr�s vite gr�ce � des tests automatiques.
- Dans ce projet : C'est l'utilisation de **Git** et **GitHub**. Quand vous faites un git push, vous int�grez votre travail. Si on avait une �quipe, GitHub v�rifierait que votre nouveau code ne casse pas le code des autres.

> CD (CONTINUOUS DELIVERY / DEPLOYMENT) :
C'est l'automatisation de la mise en production. Une fois que le code est int�gr� (CI) et valid�, il est automatiquement envoy� aux utilisateurs.
- But : Rendre les nouvelles fonctionnalit�s disponibles imm�diatement, sans intervention humaine manuelle et risqu�e.
- Dans ce projet : C'est le r�le d'**Airflow** et **Heroku**.
  1. Airflow valide que le nouveau mod�le est meilleur (Test).
  2. Si oui, il d�clenche le d�ploiement sur Heroku (Delivery).
  3. L'application est mise � jour instantan�ment pour les m�decins.

> R�SUM� SIMPLE :
- **CI** = J'int�gre mon code proprement (Git).
- **CD** = Je livre le produit fini automatiquement (Heroku).

